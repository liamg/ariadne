package version

const Name = "ariadne"

const Author = "Liam Galvin"

// Version is the git tag associated with the commit being released - set using -ldflags at compile time, hence the "var"
var Version = "dev"
